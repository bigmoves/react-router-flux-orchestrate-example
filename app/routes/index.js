/** @jsx React.DOM */

var React = require('react');

var Index = module.exports = React.createClass({
  render: function() {
    return (
      <div>
        <h1>Hello, there.</h1>
      </div>
    );
  }
});


/** @jsx React.DOM */

var React = require('react');

var UnknownRoute = module.exports = React.createClass({
  render() {
    return (
      <div>
        <h1>Not found.</h1>
      </div>
    );
  }
});

